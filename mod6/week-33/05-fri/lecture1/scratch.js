/*
We can use a list comprehension to make new arrays with one
line of logic.While these can be powerful, also take into
account readability.


    TIP:

1. if before for -> replace the el(same size list)

2. if after for -> filter list(makes resulting list smaller)

3. use the same variable for el to reference the el in new list



*/

// const myNumList = [1, 2, 3, 4, 5];

// const doubledList = [];

// for(let num of myNumList){
//     doubledList.push(num * 2);
// }

// const doubledList = myNumList.map(el => el *2);

// const twoList = myNumList.filter(el => el === 2)

// console.log(twoList);
// console.log(doubledList)
// let lst1 = [1, 2, 3]
// let lst2 = [4, 5, 6]

// console.log([...lst1, ...lst2])
